# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Vite build output
dist/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# Firebase config local (copiar de config.example.js)
src/app/config.js

# dataconnect generated files
.dataconnect

# ─────────────────────────────────────────────────────────────
# DADOS FINANCEIROS REAIS — nunca versionar
# Arquivos selecionados localmente no importador não devem
# entrar no repositório. Mantenha-os apenas no seu dispositivo.
# ─────────────────────────────────────────────────────────────
/imports-local/
/data/private/
*.real.json
*.sensitive.json
cfm_import_v1_final_perfeito_validado.json
cfm_import_2026*.json

# Screenshots locais de etapas anteriores (não versionar)
docs/screenshots-etapa4/
docs/screenshots-etapa4/**
docs/screenshots-etapa5/_invalid-temp.json
docs/screenshots-etapa5/_storage-temp.json

# Regras pessoais de classificação — nunca versionar
src/config/classification-rules.local.js
src/config/personal-rules.local.js
src/config/card-snapshots.local.js

# Qualquer JSON com sufixo indicando dados reais ou sensíveis
*_real.json
*_privado.json
*_financeiro.json
