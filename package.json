{
  "name": "cf-marc",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "firebase:login": "firebase login",
    "firebase:projects": "firebase projects:list",
    "firebase:emulators": "firebase emulators:start --only auth,database",
    "firebase:test-rules": "firebase emulators:exec --only database \"vitest run src/firebase-rules.test.ts\"",
    "firebase:deploy-database": "firebase deploy --only database --project cfmarc-marc35",
    "firebase:deploy-auth": "firebase deploy --only auth --project cfmarc-marc35"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-tools": "^15.23.0",
    "jsdom": "^29.1.1",
    "playwright": "^1.49.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
